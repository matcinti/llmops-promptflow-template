{
    "experiment": {
        "email": "${data.email}"
    },
    "evaluation": {
        "evaluation": {
            "ground_truth": "${data.truth}",
            "entities": "${run.outputs.categories}"
        }
    }

}