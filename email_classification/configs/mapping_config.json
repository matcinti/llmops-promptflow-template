{
    "experiment": {
        "email": "${data.email}"
    },
    "evaluation": {
        "evaluation": {
            "ground_truth": "${data.truth}",
            "answer": "${run.outputs.categories}"
        }
    }

}